name: family-manifest
on: [pull_request, push]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: actions/setup-node@v4
        with: { node-version: 22 }
      - run: node scripts/check-family-manifest.mjs
      # Node builtins only, so it runs in this no-install job.
      - run: node scripts/test-check-conformance-manifest.mjs
      - run: node scripts/check-conformance-manifest.mjs
